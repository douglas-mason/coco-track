import { css } from 'emotion';

export const loginFormClass = css({
  maxWidth: 300,
});

export const loginForgotClass = css({
  float: 'right',
});

export const loginButtonClass = css({
  width: '100%',
});
